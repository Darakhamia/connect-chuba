# 🚀 Connect Chuba - Инструкция по настройке

## Шаг 1: Настройка Clerk Authentication

1. Перейдите на [dashboard.clerk.com](https://dashboard.clerk.com)
2. Создайте новое приложение
3. В настройках приложения включите нужные провайдеры:
   - ✅ Email/Password
   - ✅ Google OAuth
4. Скопируйте ключи API из раздела "API Keys"

5. Обновите файл `.env`:
```env
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_ВАШ_ПУБЛИЧНЫЙ_КЛЮЧ
CLERK_SECRET_KEY=sk_test_ВАШ_СЕКРЕТНЫЙ_КЛЮЧ
```

## Шаг 2: Настройка UploadThing (загрузка файлов)

1. Перейдите на [uploadthing.com](https://uploadthing.com)
2. Создайте новое приложение
3. Скопируйте токен из Dashboard

4. Обновите файл `.env`:
```env
UPLOADTHING_TOKEN=ВАШ_UPLOADTHING_ТОКЕН
```

## Шаг 3: Запуск базы данных

База данных уже настроена для локальной разработки с Prisma PostgreSQL.

### Для запуска:
```bash
# В первом терминале - запустите базу данных
npx prisma dev --name "connect-chuba"

# В втором терминале - запустите приложение
npm run dev
```

### Альтернатива: Облачная база данных (Neon)

Если хотите использовать облачную базу:

1. Создайте аккаунт на [neon.tech](https://neon.tech)
2. Создайте новый проект
3. Скопируйте connection string
4. Обновите `.env`:
```env
DATABASE_URL="postgresql://user:password@host/database?sslmode=require"
```

5. Примените миграции:
```bash
npx prisma db push
```

## Шаг 4: Запуск приложения

```bash
# Установите зависимости (если ещё не установлены)
npm install

# Запустите dev сервер
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000) в браузере.

---

## 📋 Что уже реализовано:

### Фаза 1 ✅
- [x] Next.js 14+ с App Router
- [x] TypeScript + Tailwind CSS
- [x] shadcn/ui компоненты
- [x] Prisma схема базы данных
- [x] Discord-подобный UI shell
- [x] Тёмная/Светлая темы

### Фаза 2 ✅
- [x] Clerk аутентификация
- [x] Middleware защита роутов
- [x] Prisma PostgreSQL база данных
- [x] Создание профиля пользователя
- [x] Zustand store для модалов
- [x] Модал создания сервера
- [x] API для серверов
- [x] UploadThing для загрузки изображений
- [x] Навигация по серверам и каналам
- [x] Страницы серверов и каналов

### Фаза 3 (следующая)
- [ ] Socket.io для real-time сообщений
- [ ] Отправка и получение сообщений
- [ ] Система приглашений на сервер
- [ ] Управление участниками
- [ ] LiveKit для голосовых/видео каналов

---

## 🛠 Технологии

| Компонент | Технология |
|-----------|------------|
| Framework | Next.js 16 |
| Language | TypeScript |
| Styling | Tailwind CSS + shadcn/ui |
| Database | PostgreSQL (Prisma) |
| Auth | Clerk |
| State | Zustand |
| Files | UploadThing |
| Real-time | Socket.io (planned) |
| Voice/Video | LiveKit (planned) |

---

## 📁 Структура проекта

```
connect-chuba/
├── prisma/
│   └── schema.prisma          # Схема БД
├── src/
│   ├── app/
│   │   ├── (auth)/            # Страницы авторизации
│   │   ├── (main)/            # Основные страницы
│   │   │   └── servers/       # Страницы серверов
│   │   ├── api/               # API роуты
│   │   ├── globals.css        # Глобальные стили
│   │   ├── layout.tsx         # Root layout
│   │   └── page.tsx           # Главная страница
│   ├── components/
│   │   ├── chat/              # Компоненты чата
│   │   ├── modals/            # Модальные окна
│   │   ├── navigation/        # Навигация
│   │   ├── providers/         # Провайдеры
│   │   ├── server/            # Компоненты сервера
│   │   └── ui/                # shadcn/ui
│   ├── hooks/                 # Custom hooks
│   ├── lib/                   # Утилиты
│   └── types.ts               # TypeScript типы
└── .env                       # Переменные окружения
```

